# Luna direction-contract repair

Continue the exact direction thread. The host accepted your artifact envelope
but rejected the direction bundle on the objective finding in
`inputs/direction-repair/hard-finding.json`. The exact rejected files are under
`inputs/rejected-direction/`.

Fix every stated schema, timing, selector, security, or asset-pack defect while
preserving the concept and every unrelated directing choice. This is still the
direction/UI-vocabulary phase: do not author the film composition or motion
selectors.

Do not call any tool, shell, filesystem, network, browser, MCP, connector,
sub-agent, provider, or deployment surface. Return one artifact envelope with
the exact `baseFingerprint` and the same worker-supplied artifact contract. Use
`decision: "replace"`, declare the complete final manifest, mark unchanged files
`action: "inherit"`, and replace only corrected bytes. Return no Markdown fence
or prose outside the JSON envelope. There is no second direction repair.
