# Luna user revision

Continue the exact director thread for this film. The user's requested change is
in the embedded `inputs/revision.json`. The host's exact accepted canonical
bundle is embedded under `inputs/accepted-bundle/`; mechanically committed
derivatives are under `inputs/current/`. Approved images are attached to this
same CLI turn. Every input carries its exact logical path, byte length, and
SHA-256.

The accepted bundle remains the authoritative film when
`inputs/accepted-bundle-host.json` says the latest worker cursor is unaccepted.
That means a prior turn was consumed but its bytes failed or were not committed
by the trusted host; revise the accepted bytes while continuing the exact thread
generation.

The current HTML is a host-injected execution derivative. Use
`inputs/accepted-bundle/composition.html` as the canonical authored source and do not copy
`sequences-*.js`, host JSON islands, or sourced scripts other than
`gsap.min.js` into it; the host re-applies chosen deterministic contracts.

Do not call the shell, filesystem, network, MCP, connector, browser, todo-list,
sub-agent, or any other tool. The supplied evidence is the complete read-only
input for this turn.

Interpret the request in the context of the existing treatment and film. Preserve
unrelated directing choices, verified facts, local-only execution, duration
exactly, deterministic seeking, semantic evidence bindings, and the final
resting hold. A runtime change requires a fresh create with a new verified fact
envelope. Return the complete canonical bundle under
`deliverables/`. If the request is impossible without inventing a fact
or violating the permission boundary, explain that in
`deliverables/revision-note.md` and leave the composition unchanged.

Do not invoke the legacy planner, scaffold, critic, or repair committee. This is
a continuation of your own authored film, not a fresh rewrite.

Return exactly one artifact envelope matching the worker-supplied output schema
and exact `baseFingerprint`, with no Markdown fence or surrounding prose. When
the request changes nothing, use `decision: "keep"` and an empty `files` array.
Otherwise use `decision: "replace"`, declare the complete final manifest, mark
unchanged files `action: "inherit"` with their exact base SHA-256, and use
`action: "replace"` only for changed bytes. Never retype or base64-synthesize an
unchanged binary. The trusted worker validates and atomically materializes the
result; you do not write files yourself.
