# Luna artifact-envelope recovery

Continue the exact task from the immediately preceding turn. The trusted worker
did not accept that turn's artifact envelope; no new deliverable state was
materialized. `inputs/protocol-recovery/failure.json` is the complete bounded
receipt. Correct the envelope/protocol defect and return the requested artifact
contract once. Preserve the creative work from the prior turn.

Do not call any tool, shell, filesystem, network, browser, MCP, connector,
sub-agent, provider, or deployment surface. Return no Markdown fence or prose
outside the worker-supplied JSON schema. A security, permission, or integrity
failure is terminal and is never routed here.
