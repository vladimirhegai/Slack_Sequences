# Luna UI asset-pack hard repair

Continue the exact asset-intake thread. The trusted worker accepted your prior
artifact envelope, but the host rejected the UI pack on the objective finding
in `inputs/asset-pack-repair/hard-finding.json`. The exact rejected bytes are
under `inputs/rejected-asset-pack/`.

Fix the stated schema, selector, local-asset, markup, or security defect while
preserving every unrelated component, token, state, part, slot, variant, and
visual choice. This is still the reusable UI-vocabulary phase: do not author a
film, treatment, storyboard, marketing claim, or shot sequence.

Do not call any tool, shell, filesystem, network, browser, MCP, connector,
sub-agent, provider, or deployment surface. Return one `decision: "replace"`
artifact envelope with the exact `baseFingerprint` and worker-supplied contract.
Declare the complete final manifest, inherit every unchanged file by its exact
base SHA-256, and replace only corrected bytes. Return no Markdown fence or
prose outside the JSON envelope. There is no second asset-pack repair.
